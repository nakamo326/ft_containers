#include <map>
#include <stack>
#include <typeinfo>

#include "ft_container.hpp"

int main() {
  std::vector<int>                 v;
  std::vector<int>::const_iterator cit = v.begin();
  std::pair<int, int>              p;
  (void)p;
  return 0;
}
